# Example Filled Historical Workbooks

This folder contains example Excel outputs produced by the historicals solver.

The workbooks are grouped by the local verification run they came from because
the same ticker filenames can appear in multiple output sets.

- `verification/`: filled workbook examples from the verification run.
- `verification-formula-preserved/`: filled workbook examples from the formula-preservation verification run.
